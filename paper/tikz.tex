\documentclass[tikz,border=10pt]{standalone}
\usepackage{amssymb}
% These libraries must remain in the preamble of your main document
\usetikzlibrary{arrows.meta, positioning, calc}

\begin{document}

\begin{tikzpicture}[
    >={Stealth[round, length=3mm, width=2mm]},
    line width=1.8pt,
    >={Stealth[round, length=5mm, width=3.5mm]},
    font=\Large\bfseries
]

    % --- DEFINITIONS (Moved inside for portability) ---
    \definecolor{myred}{RGB}{165, 40, 60}
    \definecolor{myyellow}{RGB}{215, 155, 60}
    \definecolor{mygreen}{RGB}{80, 140, 80}
    \definecolor{myplane}{RGB}{240, 240, 240}
    \definecolor{mygrid}{RGB}{200, 200, 200}
    \definecolor{mygray}{RGB}{50, 50, 50}
    \definecolor{bgcream}{RGB}{250, 250, 240} 

    % --- LEFT PANEL: Independent Tasks ---
    \begin{scope}[local bounding box=leftpanel,rotate around={30:(4.5, 1.25)}]
        \node[anchor=south, font=\LARGE\rmfamily] at (4.5, 6.8) {Independent tasks};
        
        \node at (2.5, 5.2) {\Huge \textcolor{mygray}{$\Theta$}};

        % --- Plane with Grid ---
        \fill[myplane] (0,0) -- (6,0) -- (9,2.5) -- (3,2.5) -- cycle;
        
        % Grid lines
        \foreach \i in {1,2,3,4} {
           \draw[mygrid, line width=0.2pt] ($(0,0)!\i/5!(3,2.5)$) -- ($(6,0)!\i/5!(9,2.5)$);
        }
        \foreach \i in {1,2,3,4,5} {
           \draw[mygrid, line width=0.2pt] ($(0,0)!\i/6!(6,0)$) -- ($(3,2.5)!\i/6!(9,2.5)$);
        }

        \draw[myplane!80!black, line width=1pt] 
            (0,0) -- (6,0) -- (9,2.5) -- (3,2.5) -- cycle;
         
        \node[font=\LARGE\rmfamily] at (0.8, 0.3) {$\mathcal{M}$};

        % Theta_0
        \coordinate (T0) at (4.5, 4.2);
        \fill[black] (T0) circle (3pt) node[right=3pt] {$\theta_0$};

        % 1. Green Arrow
        \draw[->, mygreen] (T0) to[out=190, in=80, looseness=1.0] (1.9, 2.5)  node[left, black] {$\theta_C$}; 
        \draw[dashed,gray,line width=1pt]  (1.9, 2.5)  to (1.9,0.9) circle(2pt);

        % 2. Yellow Arrow
        \draw[->, myyellow] (T0) to[out=-60, in=-110] (7.5, 6.0) node[right, black] {$\theta_B$};
        \draw[dashed,gray,line width=1pt]  (7.5, 6.0) to (7.5, 2.0) circle(2pt);

        % 3. Red Arrow
        \draw[->, myred] (T0) to[out=-120, in=70,looseness=1.5] (6., 0.8) node[right, black] {$\theta_A$};
    \end{scope}


    % --- RIGHT PANEL: Sequential Tasks ---
    \begin{scope}[xshift=9cm, local bounding box=rightpanel,rotate around={30:(4.5, 1.25)}]
        \node[anchor=south, font=\LARGE\rmfamily] at (4.5, 6.8) {Sequential tasks};
        
        \node at (2.5, 5.2) {\Huge \textcolor{mygray}{$\Theta$}};

        % --- Plane with Grid ---
        \fill[myplane] (0,0) -- (6,0) -- (9,2.5) -- (3,2.5) -- cycle;

        \foreach \i in {1,2,3,4} {
           \draw[mygrid, line width=0.2pt] ($(0,0)!\i/5!(3,2.5)$) -- ($(6,0)!\i/5!(9,2.5)$);
        }
        \foreach \i in {1,2,3,4,5} {
           \draw[mygrid, line width=0.2pt] ($(0,0)!\i/6!(6,0)$) -- ($(3,2.5)!\i/6!(9,2.5)$);
        }

        \draw[myplane!80!black, line width=1pt] 
            (0,0) -- (6,0) -- (9,2.5) -- (3,2.5) -- cycle;

        \node[font=\LARGE\rmfamily] at (0.8, 0.3) {$\mathcal{M}$};

        % Theta_0
        \coordinate (T0_seq) at (4.5, 4.2);
        \fill[black] (T0_seq) circle (3pt) node[left=3pt] {$\theta_0$};

        % Points on plane
        \coordinate (TA_seq) at (6., 0.8);
        \coordinate (TB_seq) at (3.3, 1.8);
        \coordinate (TC_seq) at (3.5, 0.4);

        \draw[->, myred] (T0_seq) to[out=-120, in=70,looseness=1.5] (TA_seq);
        \node[above left] at (TA_seq) {$\theta_A$};

        \draw[->, myyellow] (TA_seq) to[out=-150, in=-10, looseness=1.5] (TB_seq);

        \draw[->, mygreen] (TB_seq) to[out=190, in=150, looseness=1.8] (TC_seq);
        \node[right] at (TC_seq) {$\theta_C$};

    \end{scope}

\end{tikzpicture}
\end{document}